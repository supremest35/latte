<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.latte.dao.OrderDao">

	<insert id="insertOrder" parameterType="com.example.latte.vo.Order">
		<selectKey keyProperty="no" resultType="int" order="BEFORE">
			select latte_order_no_seq.nextval 
			from dual
		</selectKey>
		insert into latte_orders
			(order_no, user_no, order_description, order_rec_no, order_amount, order_status, order_message, order_total_price)
		values
			(#{no}, #{userNo}, #{description}, #{recNo}, #{amount}, #{status}, #{message}, #{totalPrice})
	</insert>
	
</mapper>