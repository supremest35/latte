<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.latte.dao.QnaDao">
	<select id="getQnasByProfileNo" parameterType="int" resultType="com.example.latte.vo.Qna">
		select 
			qna_no					as no,
			profile_no				as profileNo,
			qna_question			as question,
			qna_answer				as answer,
			qna_created_date		as createdDate
		from 
			minihome_profile_qnas
		where 
			profile_no = #{value}
	</select>
</mapper>